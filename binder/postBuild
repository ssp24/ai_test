#!/bin/bash

# Ensure the .env file is sourced
if [ -f .env ]; then
  export $(cat .env | xargs)
fi

# Create a Jupyter notebook configuration file to set the API key
mkdir -p ~/.jupyter
echo "c.NotebookApp.env_keep.append('OPENAI_API_KEY')" >> ~/.jupyter/jupyter_notebook_config.py
